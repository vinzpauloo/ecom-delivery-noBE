import React from "react";

import styles from "./ComponentTemplate.module.scss";

interface ContainerProps {}

const ComponentTemplate: React.FC<ContainerProps> = ({}) => {
  return (
    <div>
      <h2>ComponentTemplate</h2>
    </div>
  );
};

export default ComponentTemplate;
