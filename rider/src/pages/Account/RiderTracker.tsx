import React from "react";
import RiderTrackerContainer from "../../components/Account/Tracker/RiderTrackerContainer";

type Props = {};

const Order = (props: Props) => {
  return (
    <div className="page">
      <RiderTrackerContainer />
    </div>
  );
};

export default Order;
