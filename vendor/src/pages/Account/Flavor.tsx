import React from "react";
import FlavorContainer from "../../components/Account/Flavors/FlavorContainer";

type Props = {};

const Flavor = (props: Props) => {
  return (
    <div className="page">
      <FlavorContainer />
    </div>
  );
};

export default Flavor;
