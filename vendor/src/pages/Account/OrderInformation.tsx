import React from "react";
import RiderContainer from "../../components/Account/Rider/RiderContainer";

type Props = {};

const OrderInformation = (props: Props) => {
  return (
    <div className="page">
      <RiderContainer />
    </div>
  );
};

export default OrderInformation;
