import React from "react";
import StatusContainer from "../../components/Account/Status/StatusContainer";

type Props = {};

const Status = (props: Props) => {
  return (
    <div className="page">
      <StatusContainer />
    </div>
  );
};

export default Status;
